import React from 'react';
import { View } from 'react-native';

export default function TabHomeScreen() {
  // Esta pantalla es manejada por _layout.tsx
  // El contenido real (RitmoFit) se renderiza desde allí
  return <View />;
}
